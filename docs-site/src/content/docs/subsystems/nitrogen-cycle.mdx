---
title: Nitrogen cycle
description: Waste to ammonia to nitrite to nitrate — two bacterial guilds, an oxygen bill at every step, and a colony the tank has to keep earning.
---

import { Badge } from '@astrojs/starlight/components';

## What it models

Waste becomes ammonia, ammonia becomes nitrite, nitrite becomes nitrate. Two
bacterial guilds run the last two stages, and both pay for the reaction in
dissolved oxygen — 4.57 mg of O₂ per mg of nitrogen carried the whole way, three
quarters of it on the first step. The colonies grow on the work they actually do
and fade on a maintenance clock that never stops, so a biofilter is a stock the
tank has to keep earning.

## Status

| Mechanic | Status | Why |
|---|---|---|
| Three-stage chain | <Badge text="settled" variant="success" /> | Compound mass converts through the molecular weights in `src/simulation/core/`; no tunable restates one. |
| Ammonia source | <Badge text="settled" variant="success" /> | A physical bed reserve — filled by settling waste, drained by leaching — rather than a flat ambient constant. |
| Processing capacity | <Badge text="settled" variant="success" /> | Population × per-cell throughput × warmth × air, with no volume term — the same colony clears the same mass in a nano and a 300 L. |
| Substrate limitation | <Badge text="settled" variant="success" /> | Monod on total ammonia for AOB and on nitrite for NOB, so a cycled tank holds hundredths of a ppm and a pulse stands until the colony works it down. |
| Colony growth and decay | <Badge text="settled" variant="success" /> | Logistic growth scaled by utilization; unconditional maintenance decay on a 21-day half-life. |
| Oxygen limitation | <Badge text="settled" variant="success" /> | Monod on both guilds, NOB's half-saturation nearly 4× AOB's — which is where standing nitrite comes from. |
| Temperature | <Badge text="settled" variant="success" /> | One Q10 factor across oxidation, growth and decay, because they are one metabolism. |
| Guild seeding | <Badge text="settled" variant="success" /> | A trickle per litre of water that never stops, so a guild is always present and grows the moment it has feedstock — no concentration gate. |
| Mineralization rate | <Badge text="scaffolding" variant="caution" /> | A flat fraction of standing waste with no temperature and no oxygen term, in the one aerobic step of the chain that has neither. |
| N mass conservation | <Badge text="scaffolding" variant="caution" /> | The chain conserves N per atom, but decay oxidation, plant uptake and metabolic retention are untracked sinks. The end-to-end mass accounting controls for them rather than closing them. |
| Colony sizing on inert beds | <Badge text="settled" variant="success" /> | A colony grows to the load it is fed, not to the surface under it, so an inert bed's smaller surface lowers the ceiling without setting the cycling clock. |
| Cycled seed | <Badge text="settled" variant="success" /> | A `'cycled'` colony is the one the tank's load sustains at rest — its fish fed to satiety and its bed's leach — floored at a fishless month's share of the surface ceiling. Every stocked setup opens on hundredths of a ppm; food fed past satiety is outside that load, so an overfed tank's colony grows on from the seed. |
| Nitrogen alerts | <Badge text="scaffolding" variant="caution" /> | Ammonia alerts on free NH₃ where harm starts; nitrite still alerts at 1.0 ppm where fish damage starts at any presence. |
| Bed disturbance | <Badge text="scaffolding" variant="caution" /> | Lifting hardscape stirs one slot's share of the bed per piece, and uprooting a plant its own patch: that share of the reserve comes loose as waste and its biofilm is scraped off. The bed holds only about a day of the tank's waste, so a rescape shows as a small, short blip. |
| Denitrification | <Badge text="missing" variant="danger" /> | Nitrate has no biological sink. It leaves only through plant uptake and water changes, so an unplanted tank accumulates it without bound. |
| Nitrification acidity | <Badge text="settled" variant="success" /> | The 2 H⁺ each NH₄⁺ releases spend KH at 7.14 mg CaCO₃ per mg of nitrogen, so a tank that nitrifies without water changes loses its buffer and its pH. |

## How it works

### The three stages

| Stage | Reaction | Run by |
|---|---|---|
| 1 · Mineralization | waste (g) → NH₃ (mg) | No organism. A flat fraction of standing waste, times a stoichiometric N content. |
| 2 · Ammonia oxidation | NH₄⁺ + 1.5 O₂ → NO₂⁻ + 2 H⁺ + H₂O | AOB, saturating on what is dissolved |
| 3 · Nitrite oxidation | NO₂⁻ + 0.5 O₂ → NO₃⁻ | NOB, saturating on what is dissolved |

Nitrogen is conserved atom for atom across the chain. Compound mass is not — it
grows with molecular weight, and every conversion reads its ratio from the
shared chemistry in `src/simulation/core/` so the whole engine agrees on the
arithmetic.

| Conversion | Factor | Derivation |
|---|---|---|
| NH₃ → NO₂⁻ | `2.702` | `MW_NO2 / MW_NH3` |
| NO₂⁻ → NO₃⁻ | `1.348` | `MW_NO3 / MW_NO2` |
| O₂ spent per mg NH₃ | `2.819` | `1.5 × MW_O2 / MW_NH3` |
| O₂ spent per mg NO₂⁻ | `0.348` | `0.5 × MW_O2 / MW_NO2` |
| KH spent per mg NH₃, as CaCO₃ | `5.877` | `MW_CaCO3 / MW_NH3` |

The oxygen draw is derived from the nitrogen actually oxidised, never quoted
separately. A tank that cannot run the reaction does not pay for it.

### Stocks and rates

| Stock | Fills it | Drains it | Unit |
|---|---|---|---|
| `waste` | Food decay, fish feces, fish death, plant tissue loss, substrate leaching | Mineralization to ammonia; settling into the bed | g |
| `ammonia` | Mineralization; fish gill excretion | AOB oxidation; water change removes proportional mass | mg |
| `nitrite` | AOB oxidation, × 2.702 | NOB oxidation; water change | mg |
| `nitrate` | NOB oxidation, × 1.348; fertilizer dosing | Plant uptake; water change | mg |
| `aob` | Seeding trickle; logistic growth on utilization | Maintenance decay; surface-area cap | units of 10⁶ cells |
| `nob` | Seeding trickle; logistic growth on utilization | Maintenance decay; surface-area cap | units of 10⁶ cells |
| `oxygen` | Gas exchange, elsewhere | 2.819 mg per mg NH₃ and 0.348 mg per mg NO₂⁻ oxidised | mg/L |

`waste` has exactly one drain. A water change dilutes the three nitrogen
compounds but leaves waste untouched, and no filter, vacuum or action removes
it.

### Storage is mass, not concentration

Nitrogen compounds are stored as mass in mg, and a concentration is derived from
that mass and the standing water only where a threshold or a display needs one.
Storing the conserved quantity is what makes evaporation concentrate the tank for
free: same mass, less water, higher ppm, no code. Oxygen is the exception — it is
stored as a concentration, so both oxidation steps divide their oxygen bill by
standing water before pushing the effect.

On a drained tank that derivation returns 0, and both oxidation stages are gated
on `water > 0`: nitrifiers oxidise what is dissolved. Maintenance decay sits
outside that gate on purpose, so a colony in a drained tank dies back rather
than waiting.

### Processing capacity

```
capacity    = population × processingRate × warmth × air
consumed    = capacity × S / (K + S)         // S: concentration the tick ends on
utilization = consumed / capacity            // 0..1, dimensionless
```

Each guild saturates on its own substrate as well as on oxygen: AOB on total
ammonia, NOB on nitrite. Capacity is the maximum a colony reaches in a glut; in
ordinary water it runs at a Monod share of it, so a colony works in proportion
to what is dissolved rather than at a fixed pace up to its limit.

The curve is read at the concentration the tick ends on — the root of
`consumed = capacity × monod(standing − consumed)`. Read at the start instead, a
colony with more throughput per hour than the half-saturation mass would empty
the pool every tick and a cycled tank would read exactly zero. Read at the end,
uptake never exceeds either the stock or the capacity, and a steady state rests
where the continuous curve puts it.

| Guild | Substrate | Half-saturation `K` | Source |
|---|---|---|---|
| AOB | NH₃ + NH₄⁺, as NH₃ | `0.61` ppm (0.5 mg/L N) | *Nitrosomonas*, low end of wastewater models' 0.5–1 mg/L N |
| NOB | NO₂⁻ | `0.66` ppm (0.2 mg/L N) | *Nitrospira*, the guild that dominates aquarium biofilters, 0.1–0.3 mg/L N |

Capacity carries no volume term. Throughput is a property of the cell, so the
same colony clears the same milligrams in a nano and in a 150 L — which is what
makes a ppm reading fall with volume the way it does in a real tank.

NOB use the same gauge scaled by ≈ `2.951`. That is the 2.702 mass ratio
re-quoted through each guild's own oxygen factor at air saturation, rather than
the bare ratio, which would hand NOB AOB's correction on top of their own and
leave them 8.4 % under parity.

### Colony dynamics

| Flow | Form | Gated on |
|---|---|---|
| Seeding | `water × seedingRate`, both guilds | Standing water; held under the surface ceiling |
| Growth | `pop × growthRate × warmth × air × utilization × (1 − pop/max)` | Utilization — nothing to oxidise means no growth |
| Decay | `pop × deathRate × warmth` | Nothing. It is unconditional |
| Surface cap | Population truncated to `surface × bacteriaPerCm2` | Surface leaving the tank — a rescape, a media change |
| Disturbance | The disturbed share of the bed's colony scraped off; a lifted piece takes its own biofilm | A keeper digging into the bed |

Seeding is how a colony starts: cells settle out of the fill water and the air
every tick, so a guild is never absent and emerges on exponential growth the
moment its feedstock appears. With nothing to eat, a guild idles at
`seedingRate / deathRate` — about 73 cells/mL. The rate is the cycling clock,
and logarithmic in it: each tenfold cut adds three to four days.

`air` scales oxidation and growth alike, because a colony cannot divide on a
reaction it cannot run. It deliberately does not scale maintenance decay, which
is what makes an anoxic tank lose its biofilter rather than merely pause it.

Decay is maintenance loss, not starvation. Bacteria fade over weeks rather than
collapsing when the meal ends, which is why a tank survives a holiday.

A colony under a steady load settles where the two flows cancel:

```
utilization_rest = deathRate / (growthRate × air × (1 − pop/max))
```

In air-saturated water with the ceiling far off, that is **4.0 % for AOB** and
**7.1 % for NOB**. The rest point moves with oxygen, and not with temperature —
`warmth` cancels out of it, because a cell that oxidises half as fast also
divides and starves half as fast.

Utilization is the substrate curve, so the rest point fixes the concentration a
cycled tank holds: `S = K × u / (1 − u)` — about 0.03 ppm ammonia and 0.05 ppm
nitrite, almost whatever the load. A heavier load grows a bigger colony, not a higher
reading. A pulse lifts `S` and with it the colony's pace, so it clears in hours
on a mature colony with headroom and in days on one still growing into its load.

### Oxygen: why nitrite stands

| Guild | Half-saturation `K` | Rate left at 8.38 mg/L | Rate left at 0.10 mg/L |
|---|---|---|---|
| AOB | `0.30` mg/L | 96.5 % | 25.0 % |
| NOB | `1.10` mg/L | 88.4 % | 8.3 % |

Every base rate multiplied by that factor is a Monod **maximum**, reached only
at infinite oxygen. The three nitrifier rates divide the shortfall back out, so
what a tank at air saturation reproduces is the quoted 20 h doubling rather than
the 20.7 h an uncorrected constant would give.

The ratio between the two guilds runs from `1.000` at air saturation to `0.364`
at 0.10 mg/L. That gap is the whole story of standing nitrite: a tank short of
air goes on oxidising its ammonia long after it has stopped clearing the nitrite
that ammonia becomes.

Oxygen, not surface, is the binding constraint on a mature colony. Held under a
saturating dose, a bare 200 L settles at 95 % of its surface ceiling for AOB and
53 % for NOB — against 96 % / 94 % with the oxygen term switched off.
Circulation decides how far short a guild stops: NOB run from 1.4 % with nothing
moving the water to 90.1 % on a canister, an air pump and a powerhead.

### Temperature

```
warmth = q10 ^ ((temperature − referenceTemp) / 10)
```

One metabolism, one factor: oxidation, growth and maintenance all carry it. A
cold tank therefore needs a larger colony to clear the same load and takes
longer to build it — an 18 °C cycle runs about twice the days a 25 °C one does —
while the utilization the colony rests at does not move.

### Where the ammonia comes from

The bed. Its organic reserve is a living store: a fresh substrate starts with a
fixed amount per litre, a fraction of whatever it holds leaches back out as
waste each tick, and a share of the tank's standing waste settles into it as
mulm. Settling is fastest in still water and halves at `settlingHalfTurnover`
tank turnovers an hour, since flow keeps particles suspended; a bare bottom
holds nothing. Settling and leaching move grams of waste gram for gram, so the
nitrogen in the bed is the nitrogen that left the water.

A fresh bed's reserve tapers to where settling balances the leach, roughly a
day of the tank's waste production. A water change can vacuum the bed, taking
a share of the reserve out of the tank.

| Substrate | Colony surface | Organic reserve |
|---|---|---|
| `none` | `0` cm²/L | `0` g/L |
| `sand` | `400` cm²/L | `0.011` g/L |
| `gravel` | `800` cm²/L | `0.013` g/L |
| `aqua_soil` | `1200` cm²/L | `0.05` g/L |

Surface and reserve are separate properties of the same bed: surface is the
colony ceiling, reserve is the ammonia source.

Digging into the bed stirs a share of it: that share of the reserve comes loose
as waste, and that share of the bed's biofilm is scraped off.

| Move | Share of the bed disturbed |
|---|---|
| Uprooting a rooted plant | one plant slot — `1 / max plants`; an epiphyte disturbs nothing |
| Lifting a piece of hardscape | one hardscape slot — `1 / slots`; the piece's own biofilm leaves with it |
| Setting a piece down | nothing; it arrives sterile |
| Swapping the bed | all of it — the colony on it goes out with it and the new bed brings a fresh reserve |

A release lifts the concentration the colony works against, so it stands as a
bump until worked down. An established bed holds only its mulm, so rescaping a
cycled tank shows as a small, brief bump; digging up most of the bed at once
shows more. An inert bed leaches less than soil does, so its colony grows to a
smaller load, but it cycles on the same seeding clock. A `'cycled'` seed skips
the clock: each guild opens at the population where growth on its resting load
meets decay, never below a fishless month's share of the ceiling. That load
counts what the fish eat, not food fed past satiety, so a tank whose keeper
overfeeds grows its colony on from the seed.

## Key tunables

| Constant | Meaning | Unit |
|---|---|---|
| `wasteConversionRate` | Share of standing waste mineralized per tick | `0.3` /tick |
| `wasteToAmmoniaRatio` | NH₃ yielded per gram of waste — waste is ≈ 5 % N by dry mass | `60` mg/g |
| `bacteriaProcessingRate` | NH₃ one bacteria unit oxidises per tick, Monod maximum | `2.07e-4` mg/unit/tick |
| `seedingRate` | Nitrifiers settling into the tank per litre of water, each guild | `0.0001` units/L/tick |
| `aobGrowthRate` | Per-capita growth at full utilization — a 20 h doubling | `0.035898` /tick |
| `nobGrowthRate` | Per-capita growth at full utilization — a 36 h doubling | `0.021781` /tick |
| `bacteriaPerCm2` | Biofilm carrying capacity — 10⁷ cells/cm² | `10` units/cm² |
| `bacteriaDeathRate` | Maintenance loss — a 21-day half-life | `0.0013753` /tick |
| `q10` | Factor every nitrifier rate multiplies by per 10 °C | `2.5` |
| `referenceTemp` | Temperature the nitrifier rates are quoted at | `25` °C |
| `aobOxygenHalfSaturation` | O₂ at which AOB run at half rate | `0.3` mg/L |
| `nobOxygenHalfSaturation` | O₂ at which NOB run at half rate | `1.1` mg/L |
| `aobAmmoniaHalfSaturation` | Total ammonia at which AOB run at half rate | `0.61` ppm |
| `nobNitriteHalfSaturation` | Nitrite at which NOB run at half rate | `0.66` ppm |
| `AIR_SATURATED_O2` | The water every rate above is quoted in | `8.38` mg/L |
| `substrateLeachRate` | Share of the bed's remaining reserve released per tick | `0.003` /hr |
| `wasteSettlingRate` | Share of standing waste settling into the bed per tick in still water | `0.1` /hr |
| `settlingHalfTurnover` | Tank turnovers an hour at which flow halves settling | `4` /hr |

A **bacteria unit is 10⁶ cells**, which is what makes `bacteriaPerCm2` a biofilm
density you can look up rather than a score. Throughput, ceiling density and
seeding rate carry an exact gauge symmetry — three numbers, two physical degrees of
freedom — so one of them is a units convention, and the ceiling density is the
one pinned to a real measurement.

## Seams

| Neighbour | Read | Written |
|---|---|---|
| [Water & gases](/subsystems/water-and-gases/) | `oxygen` — sets both guilds' oxygen Monod factor, and with it their oxidation and growth | `oxygen` — the derived draw of both oxidation steps; `kh`, spent by ammonia oxidation |
| [Environment](/subsystems/environment/) | `water` for ppm and the submerged gate, `temperature` for the Q10 factor | — |
| [Equipment](/subsystems/equipment/) | `surface` — the colony ceiling, from filter media, substrate, hardscape and glass | `waste` — settles into the substrate's organic reserve and leaches back out of it |
| [Livestock](/subsystems/livestock/) | — | Fish write `ammonia` directly through gill excretion, and `waste` as feces and death mass |
| [Plants](/subsystems/plants/) | — | Plants drain `nitrate` on uptake and add `waste` from tissue loss |
| [Algae](/subsystems/algae/) | — | Algae reads `nitrate` as a growth benefit but consumes none of it |
| [Actions](/subsystems/actions/) | — | A water change removes `ammonia`, `nitrite` and `nitrate` mass proportionally, and a gravel vac a share of the bed's reserve; dosing adds `nitrate`; uprooting a plant disturbs the bed |
| [Alerts & logging](/subsystems/alerts-and-logging/) | — | Free NH₃ > 0.02 ppm, nitrite > 1.0 ppm and nitrate > 80 ppm each raise an alert once per crossing |

## Source

`src/simulation/systems/` — with tunables in `config/`, molecular weights and
rate shapes in `core/`, the mass ↔ ppm conversion in `resources/`, the bed in
`equipment/`, and the cross-system accounting in `tests/`.
