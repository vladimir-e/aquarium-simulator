/**
 * Default saturation cap for the vitality surplus reserve bank (half the
 * condition scale). Shared by the fish, plant, and algae configs so all
 * three start from one ceiling; each can be tuned apart later.
 */
export const SURPLUS_CAP_DEFAULT = 50;
