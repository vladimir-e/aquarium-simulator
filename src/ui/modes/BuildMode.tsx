import React from 'react';
import { ModePlaceholder } from './ModePlaceholder';

export function BuildMode(): React.JSX.Element {
  return (
    <ModePlaceholder
      title="Build"
      note="Configure the tank — equipment, scape, and stocking. Coming in this redesign."
    />
  );
}
