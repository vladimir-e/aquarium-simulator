/**
 * The four-way health vocabulary shared across the Run instrument surface:
 * sparklines, condition bars, status words, and alert outlines all speak it.
 */
export type Status = 'ok' | 'warn' | 'alert' | 'neutral';
